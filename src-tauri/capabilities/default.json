{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main","preview-window"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "store:default"
  ]
}
